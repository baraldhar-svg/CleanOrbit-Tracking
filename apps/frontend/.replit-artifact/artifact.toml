kind = "web"
previewPath = "/"
title = "FleetSaaS — School Bus Tracker"
version = "1.0.0"
id = "artifacts/fleetsaas"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 18789

[services.development]
run = "pnpm --filter @workspace/fleetsaas run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/fleetsaas", "run", "build" ]
publicDir = "artifacts/fleetsaas/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "18789"
BASE_PATH = "/"
